# Installation (planned)

## Targets
- Windows 10/11 x64

## Packaging strategy
- `Teacher.msi`
- `Student.msi`
- Optional bootstrapper bundle for one-click deployment.

## Build entry point
Use `build/build.ps1` once .NET SDK is available on build machine.
