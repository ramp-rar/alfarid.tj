<Window x:Class="Alfarid.Teacher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Alfarid Teacher" Height="720" Width="1200" Background="#1E2228">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Session: Classroom A" Foreground="White" FontSize="18" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Button Content="Start" Margin="8,0,0,0" Padding="12,6" />
                <Button Content="Stop" Margin="8,0,0,0" Padding="12,6" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="#272C34" CornerRadius="8" Padding="12">
            <ItemsControl ItemsSource="{Binding Students}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="220" Height="120" Margin="6" Background="#323A45" CornerRadius="8" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="14" />
                                <TextBlock Text="{Binding Machine}" Foreground="#D9DCE1" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding PingMs, StringFormat=Ping: {0}ms}" Foreground="#D9DCE1" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding Status}" Foreground="#F9D65C" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</Window>
